# Security

Never execute generated code automatically from untrusted input. Review generated source before compiling or running it.

Never commit passwords, API keys, access tokens, certificates, or other secrets.

Use GitHub's private security reporting features when available for sensitive vulnerabilities.
