# Debugging

1. Confirm Python 3.10+.
2. Activate the virtual environment.
3. Run a documented example.
4. If parsing fails, simplify the instruction.
5. Verify the target name.
6. Run `pytest`.

Rejecting unknown instructions is intentional: guessed code is harder to debug than an explicit error.
