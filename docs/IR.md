# Universal Intermediate Representation

Current operations:

- `print`: output a literal value
- `assign`: create/assign a variable
- `if_print`: compare a variable and print when true
- `repeat_print`: repeat output a fixed number of times
- `create_table`: represent SQL table creation

The IR is intentionally small in v0.1 so it can be tested and extended safely.
